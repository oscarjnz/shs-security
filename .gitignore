# Dependencies
node_modules
agent/node_modules

# Build output
dist
agent/dist

# Environment variables (NEVER commit)
.env
.env.local
agent/.env
agent/.env.local
*.local

# TypeScript build info
*.tsbuildinfo

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/settings.json
.idea

.vercel

# Claude Code session metadata (local only)
.claude/

# Agent runtime artifacts (created by install-windows-service.ps1)
agent/run-agent.cmd
agent/agent.log

# Author working files (notes, scripts, video drafts) — not part of the product
*.docx
*.doc
~$*
PROMPT_CLAUDECODE.md
